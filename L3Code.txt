program myprogram;

  const MAXLEVEL := 10000;
  const      MINLEVEL := 9;
   const     HEALTH := 12;
   const     BLOODPRECUDE := 15;


  /* comment are not specified in the grammar */
  
  var age,
      Firstname,
      LastName,
      BloodType,
      radius, height, area ;


  begin
    write("ilyas \ll kki");
    Firstname := "Ilyas";
    LastName := "Mohammed";
    BloodType := "0-";
    read(age); /* U can read multiple value */
    read(Firstname, LastName, BloodType);
    
    if age > 21 then
      begin
        write("U can enter");
      end
    else
      begin
        write("U can't enter");
      end
      

    write(Firstname);
    write(LastName);
    write(BloodType);
    

    var pi := 3;

    radius := 5 ;
    height := 10 ;
    area := pi * radius * radius + 2 * pi * radius * height ;
    write(area);
      
    write("Count to 10");
    /* just to test the loop */
    var i:= 20;
    while i < 10 do
      begin
        write(i, " has been writen !!");
        i := i - 1;
      end
    /* Condition */
    if radius = 0 then
      begin
        write(radius," = 0");
      end
    else
      begin
        if age <> 0 then
          begin
            write(radius, "is not null");
          end
        else
          begin
            if age > 0 then
              begin
                write(radius, "is positif not equale to 0");
              end
            else
              begin
                if age < 0 then
                  begin
                    write(radius, " is negatif not equale to 0");
                  end
                else
                  begin
                    if age >= 0 then
                      begin
                        write(radius, "is positif or equale to 0");
                      end
                    else
                      begin
                        if age >= 0 then
                          begin
                            write(radius, "is positif or equale to 0");
                          end
                        else
                          begin
                            write("if u found your self here good luck you defeated the limitaion of L3Lang programming languge");
                          end
                      end 
                  end
              end
          end
      end

/* The same condition work in loops */
/* and yes there is nested loop but the prof did't give us the depth of it.*/

  end
  