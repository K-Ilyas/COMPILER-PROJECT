program ilyasprogasd;
  const MAXLEVEL := 10000;
  const MINLEVEL := 9;
  const HEALTH := 12;
  const BLOODPRECUDE := 15;

  var age,
      Firstname,
      LastName,
      BloodType,
      radius, height, area , counter;
      /*this is comment*/
  begin

    Firstname := "Ilyas";
    LastName := "Moham\nmed";
    LastName := 12;
    BloodType := 10;
    
    /* This is a new Comment about how to create a compiler */

    write(Firstname,LastName,BloodType);

    write("you can't\n\n");
    counter := 10;
    read(counter,BloodType);

    if age > 21 then
      begin
        write("U can enter");
      end
    else
      begin
        write("U cant enter");
      end
    
    for z := 12 to 20
    begin
      BloodType := BloodType -1;
      counter := counter -1;
    end

    write(BloodType,counter);
    write(Firstname);
    write(LastName);
    write(BloodType);

  end


